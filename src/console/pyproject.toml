[project]
name = "console"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rich>=13.9.4",
    "textual>=3.0.0",
]

[dependency-groups]
dev = [
    "textual-dev>=1.7.0",
]
